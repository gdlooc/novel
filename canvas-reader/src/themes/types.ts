// Re-export render theme types for convenience
export type { RenderTheme } from '@engine/render/types';
export {
  LIGHT_THEME,
  DARK_THEME,
  SEPIA_THEME,
  ALL_THEMES,
  getThemeById,
  getDefaultTheme,
} from '@engine/render/ThemeApplicator';
