# 项目语言规范

## 简体中文要求

在本项目的所有工作中，**必须使用简体中文**，具体规则如下：

### 使用中文
- **聊天回复**：与用户的所有对话使用简体中文
- **思考过程**：`<thinking>` 标签内的推理过程使用简体中文
- **代码注释**：所有注释使用简体中文（包括 `//`、`/* */`、`/** */`、`#` 等）
- **Git 提交信息**：使用简体中文
- **文档文件**：Markdown、README 等使用简体中文
- **标记注释**：TODO、FIXME、NOTE 等使用中文描述

### 例外（保留英文）
- 代码标识符（变量名、函数名、类名等）
- 通用技术术语（API、HTTP、JSON、SQL 等）
- 面向开发者的日志输出
- 配置键值、环境变量名
- 文件路径、URL
- 第三方库/框架名称
- 正则表达式
- 命令行指令及参数

### 优先级
此中文规范为最高优先级，与其他格式规范冲突时以此为准。
